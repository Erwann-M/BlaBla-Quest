{% extends 'base.html.twig' %}

{% block title %}
Recherche pour {{ searchTerm }}
{% endblock %}

{% block body %}
<div class="container">
    <h1>Résultat pour la recherche "{{ searchTerm }}" :</h1>
    <ul>
    {% for user in results %}        
        <a class="btn mt-3 mb-3 btn-lg btn-dark" href="{{ path('user_read', {id:user.id}) }}">
            {{ user.nickname }}
        </a>
    {% endfor %}
    </ul>
</div>
{% endblock %}
